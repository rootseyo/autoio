# AutoIO v1.3.0

이번 릴리스는 매크로 반복 재생 사이 대기 시간(Repeat delay) 설정 기능을 추가하고, 모든 I/O 동작을 실시간으로 확인할 수 있는 상세 Activity 로그와 UI 성능 최적화를 적용했습니다.

## 주요 변경 사항

- **반복 재생 딜레이(Repeat delay)**: 매크로 반복 실행 시 각 루프 사이에 대기할 시간(`ms` 단위)을 설정할 수 있습니다.
- **실시간 I/O Activity 로그**:
  - 매크로 녹화 및 재생 시 마우스 이동, 마우스 클릭/스크롤, 키보드 입력(키 눌림/뗌) 이벤트를 Activity 로그에 상세하게 기록합니다.
  - 오토 클릭 실행 시 클릭 횟수 및 현재 클릭 좌표를 실시간으로 표시합니다.
- **UI 로그 렌더링 최적화**: 많은 이벤트가 빠르게 발생할 때도 UI가 버벅이지 않도록 로그를 일괄 갱신(배치 처리)합니다.
- **포커스 시 키보드 입력 기록 개선**: 앱 창에 포커스가 있는 상태에서도 키보드 녹화 이벤트가 누락 없이 정상 기록됩니다.

## 품질 개선

- 반복 딜레이 및 I/O 이벤트 설명 포맷터에 대한 단위 테스트를 추가했습니다.
- macOS와 Windows 환경에서 테스트 및 린트 검사를 통과했습니다.

## 설치 안내

- macOS: `AutoIO-macOS.zip`을 내려받아 압축을 풀고 `AutoIO.app`을 Applications 폴더로 옮깁니다.
- Windows: `AutoIO-Windows.exe`를 내려받아 실행합니다.
- macOS에서는 처음 실행한 뒤 시스템 설정의 **개인정보 보호 및 보안 → 손쉬운 사용**과 **입력 모니터링**에서 AutoIO를 허용해야 합니다.

배포 파일은 공인 인증서로 서명되지 않았으므로 운영체제의 보안 경고가 표시될 수 있습니다. 이 저장소의 Releases에서 받은 파일인지 확인한 뒤 실행하세요.
