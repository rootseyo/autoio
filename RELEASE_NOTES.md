# AutoIO v1.2.0

이번 릴리스는 오토 클릭 기능을 확장하고, macOS 26에서 발생하던 시작 크래시를 해결하며, 주요 기능을 더 명확하게 구분하도록 UI를 개선했습니다.

## 주요 변경 사항

- **Record & playback**과 **Auto click**을 각각 독립된 카드 영역으로 구분했습니다.
- 오토 클릭에 **Fixed position**과 **Free click** 모드를 제공합니다.
- 클릭 횟수와 `1~60000ms` 범위의 클릭 간격을 설정할 수 있습니다.
- 클릭 및 매크로 반복 횟수에 `0` 또는 `-1`을 입력하면 무제한으로 실행합니다.
- `F10`을 누르면 오토 클릭을 시작하고, 다시 누르면 정지합니다. 시작 대기 중에는 같은 키로 취소할 수 있습니다.
- Record, Play, Auto Click 단축키를 앱에서 변경할 수 있습니다.

## macOS 안정성

- macOS 26에서 키보드 입력 소스 API를 백그라운드 스레드에서 호출해 `SIGTRAP`으로 종료되던 문제를 수정했습니다.
- 키보드 레이아웃 정보를 메인 스레드에서 준비해 전역 키보드 리스너와 재생 컨트롤러가 안전하게 재사용합니다.

## 품질 개선

- macOS 입력 소스 처리와 F10 토글 동작에 대한 회귀 테스트를 추가했습니다.
- macOS와 Windows에서 테스트, 컴파일, Ruff 검사를 수행하는 CI를 유지합니다.

## 설치 안내

- macOS: `AutoIO-macOS.zip`을 내려받아 압축을 풀고 `AutoIO.app`을 Applications 폴더로 옮깁니다.
- Windows: `AutoIO-Windows.exe`를 내려받아 실행합니다.
- macOS에서는 처음 실행한 뒤 시스템 설정의 **개인정보 보호 및 보안 → 손쉬운 사용**과 **입력 모니터링**에서 AutoIO를 허용해야 합니다.

배포 파일은 공인 인증서로 서명되지 않았으므로 운영체제의 보안 경고가 표시될 수 있습니다. 이 저장소의 Releases에서 받은 파일인지 확인한 뒤 실행하세요.
